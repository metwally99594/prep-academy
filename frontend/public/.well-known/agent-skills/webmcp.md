# WebMCP

When supported by the browser, Prep Academy exposes safe read-only discovery tools through `navigator.modelContext.provideContext()`.
Tools are optional and the site remains fully functional in browsers without WebMCP support.
